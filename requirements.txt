# Requirements for omega_analysis_final.py
# Python 3.11+ recommended

# Core dependencies (required)
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0

# Note: LaTeX compilation (for paper) requires system installation of:
# - pdflatex (TeX Live, MiKTeX, or MacTeX)
# - bibtex
